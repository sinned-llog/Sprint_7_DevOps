apiVersion: v1
kind: Service
metadata:
  labels:
    app: wordpress
  name: wordpress
  namespace: datascientest
spec:
  type: NodePort
  selector:
    app: wordpress
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
      nodePort: 30001